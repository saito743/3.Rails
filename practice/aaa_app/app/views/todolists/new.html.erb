<h1>新規作成</h1>
<!-- form_for↓ ＠list(インスタンス変数)はここでも使用可能-->
<%= form_for(@list, url: '/todolists') do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <%= f.submit '投稿' %>
<% end %> <!-- ブロック変数|f|はここまで有効 -->